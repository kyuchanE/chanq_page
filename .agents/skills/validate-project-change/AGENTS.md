# Validation Skill Maintenance Instructions

Keep this skill read-only unless the user separately requests fixes. Update its checks when package scripts, architecture enforcement, content validation, or infrastructure tooling changes. Preserve the distinction between passed, failed, skipped, and unavailable checks.
