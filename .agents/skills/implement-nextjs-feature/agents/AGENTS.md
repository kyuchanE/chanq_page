# Skill Metadata Instructions

Keep `openai.yaml` aligned with the parent `SKILL.md`. Quote string values, keep the short description concise, and ensure `default_prompt` explicitly invokes `$implement-nextjs-feature`. Do not add optional metadata without a real requirement.
