# Skill Metadata Instructions

Keep `openai.yaml` aligned with the parent `SKILL.md`. Quote string values, keep the short description concise, and ensure `default_prompt` explicitly invokes `$plan-project-change`. Do not add icons, colors, dependencies, or policy fields without a real requirement.
